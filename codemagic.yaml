workflows:
  android-build:
    name: Focus Lock APK Build
    max_build_duration: 30
    instance_type: mac_mini_m1
    scripts:
      - name: Build Android APK
        script: |
          gradle build
    artifacts:
      - app/build/outputs/apk/**/*.apk
      - app/build/**/*.apk
      - build/**/*.apk
    publishing:
      email:
        recipients:
          - bilashhossen8989@gmail.com
